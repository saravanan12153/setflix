<header id= "content-header">
  <h1><%= pluralize(@genres.size, 'Genre') %></h1>
</header>

<ul id="genres">
  <% @genres.each do |genre| %>
    <li>
      <%= genre.name %>
    </li>
  <% end %>
</ul>
<%= link_to 'Crate New genre', new_genre_path, class: 'button' %>
